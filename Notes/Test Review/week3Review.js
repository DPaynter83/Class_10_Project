// =========================
// Functions & Basics
// =========================

// 1) Write a function greet() that logs "Hello World".

// 2) Write a function add(a, b) that returns the sum of a and b.

// 3) Write a function called multiply(x, y) that returns the product.
//    Test it with multiply(4, 5).

// 4) Write a function getElement(arr, index) that returns the element at that index.
//    Example: getElement(["a", "b", "c"], 1) → "b"

// 5) Recursion: Write a recursive function factorial(n) that returns n! (factorial).


// =========================
// Callbacks (Sync & Async)
// =========================

// 6) Write a function outer(callback) that logs "Start", calls the callback, then logs "End".
//    Test with a callback that logs "Inside".

// 7) Write a function calculate(a, b, callback) that applies the callback to a and b.
//    Test with addition and subtraction.

// 8) Asynchronous: Use setTimeout to log "This runs after 2 seconds".


// =========================
// Arrow Functions
// =========================

// 9) Rewrite this as an arrow function:
// function square(n) { return n * n; }

// 10) Write an arrow function helloName(name) that logs "Hello <name>".


// =========================
// Array Methods
// =========================

// 11) Use forEach to log each fruit in ["apple", "banana", "pear"].

// 12) Use map to create a new array of doubled numbers from [1, 2, 3, 4].

// 13) Use filter to create a new array of only even numbers from [1, 2, 3, 4, 5, 6].

// 14) Use reduce to find the sum of [5, 10, 15].

// 15) Use sort to order [100, 2, 25, 5] in ascending order.


// =========================
// OOP (Classes & Inheritance)
// =========================

// 16) Create a class Person with properties name and age.
//     Add a method introduce() that logs "Hi, I'm <name> and I'm <age> years old."

// 17) Create a class Animal with a method speak() that logs "This animal makes a sound".
//     Create Dog extends Animal and override speak() → "The dog barks".
//     Create a Dog and call speak().

// 18) Create a class User with name and email.
//     Create an Admin that extends User and overrides login() to log
//     "<name> has logged in as Admin".
//     Create instances and demonstrate.
